const age=15;

age>=18 ? console.log("Adult") : console.log("Child");

const group = age>=18 ? 'Adult' : 'Child';
console.log(group);

console.log(`I belongs to group ${age>=18 ? 'Adult' : 'Child'}`);